# ark_server_update_script
Shell script for ark server update on SteamCmd of linux

Locate the script to same directory of steamcmd.sh

Put following command at the directory of the script.

`sh update_ark.sh`